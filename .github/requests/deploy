adopt-only: record the AikiriLedger this wallet already deployed (the first deploy run sent the transaction, then failed on a lagging RPC read before writing config). Never send a second deploy. Removed by the run.
