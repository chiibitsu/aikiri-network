adopt-only: record the AikiriLedger this wallet already deployed. Attempt 2: log lookup bounded to the creation block (public RPC rejects wide ranges). Never send a second deploy. Removed by the run.
