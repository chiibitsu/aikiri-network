*.key
*.env
__pycache__/
.pytest_cache/
*.egg-info/
build/
dist/
ledger/blocks/*.tmp
# `ots upgrade` leaves the old proof as <name>.bak; `git add ledger/proofs` must not take it
ledger/proofs/*.bak
ledger/proofs/*.tmp

# the compiled approval signer, and anything else built locally
/aikiri-approve
*.entitlements
.venv/
venv/
